# 统一依赖（建议 Python 3.10）
# 说明：
# - 该文件目标是“一套环境可直接跑三个模式”；
# - TensorFlow 采用 2.15.x，避免与 mediapipe 的 protobuf 约束冲突。

# 基础
numpy==1.26.4
opencv-python==4.10.0.84
mediapipe==0.10.14
requests>=2.31,<3.0

# MTCNN
torch>=2.1,<2.11
torchvision>=0.16,<0.21
facenet-pytorch==2.6.0

# 多检测器
retina-face==0.0.17
tensorflow==2.15.1
insightface==0.7.3
onnxruntime==1.19.2
ultralytics==8.3.0
gdown>=5.2,<6.0

# SAM（RUN_MODE=sam_mask）
# 若 git 网络慢，可按 face_detection/README.md 用 zip 本地安装 MobileSAM。
git+https://github.com/ChaoningZhang/MobileSAM.git
