# Colab 专用依赖（面向 Python 3.12）
# 说明：
# - 目标是“先在 Colab 跑通主流程”，不追求与本地环境完全一致；
# - 默认绕开 tensorflow / retina-face，因为它们在 Colab(Python 3.12) 下最容易失败；
# - 默认不安装 torch / torchvision，优先复用 Colab 预装版本；
# - 默认不安装 MobileSAM / optimum-quanto / torchao，减少首轮环境冲突。
# - facenet-pytorch==2.6.0 约束 Pillow<10.3，因此这里与本地版分开处理。

# 基础
numpy==1.26.4
opencv-python==4.10.0.84
mediapipe==0.10.14
requests>=2.31,<3.0
Pillow>=10.2,<10.3

# 检测侧（不依赖 TensorFlow）
facenet-pytorch==2.6.0
insightface==0.7.3
onnxruntime==1.19.2
ultralytics==8.3.0
gdown>=5.2,<6.0

# 生成侧（先保留主流程依赖）
diffusers>=0.31,<0.36
transformers>=4.44,<4.51
accelerate>=0.34,<2.0
peft>=0.12,<0.18
safetensors>=0.4,<0.6
huggingface-hub>=0.26,<1.0

# 以下依赖故意不放进 Colab 版本：
# retina-face==0.0.17
# tensorflow==2.15.1
# git+https://github.com/ChaoningZhang/MobileSAM.git
# optimum-quanto>=0.2.6,<0.3
# torchao>=0.10,<0.12
#
# 如需补装：
# 1. RetinaFace/TensorFlow：建议改到独立的 Python 3.10 环境运行；
# 2. MobileSAM：仅在 RUN_MODE=sam_mask 时按 detection/README.md 补装；
# 3. 量化优化：主流程跑通后，再按实际模型需求补装 optimum-quanto / torchao。
